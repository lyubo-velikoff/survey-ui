/**
 * Action types import
*/
import {
} from '../actions/types'

/**
 * Reducer initial state
 */
const initialState = {}

/**
 * General Reducer
 * @param { Object } state 
 * @param { Object } action 
 */
export default (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}